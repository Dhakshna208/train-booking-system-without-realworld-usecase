# Docker app port exposed on your host
PORT=5000

# IMPORTANT: in Docker compose, use service name `mongo` (not localhost)
MONGO_URI=mongodb://mongo:27017/train_booking

# Replace with a strong secret in production
JWT_SECRET=your_super_secret_key
